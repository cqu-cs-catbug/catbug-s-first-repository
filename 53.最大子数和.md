## 53.最大子数和

给你一个整数数组 `nums` ，请你找出一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。

**子数组**是数组中的一个连续部分。

 

**示例 1：**

```
输入：nums = [-2,1,-3,4,-1,2,1,-5,4]
输出：6
解释：连续子数组 [4,-1,2,1] 的和最大，为 6 。
```

**示例 2：**

```
输入：nums = [1]
输出：1
```

**示例 3：**

```
输入：nums = [5,4,-1,7,8]
输出：23
```

 

**提示：**

- `1 <= nums.length <= 105`
- `-104 <= nums[i] <= 104`

 

**进阶：**如果你已经实现复杂度为 `O(n)` 的解法，尝试使用更为精妙的 **分治法** 求解。



```cpp
class Solution //使用动态规划
{
public:
    int maxSubArray(vector<int>& nums) {
        int pre = 0, end = nums[0];
        for (const auto &x: nums) { //表示以只读的方式遍历nums中的元素
                           //其中，const表示不变，auto表示自动推导数据类型，&表示引用、不会拷贝数组
            pre = max(pre + x, x);//如果前面的数组“拖了后腿”，就重新开始计算
            end = max(end, pre);//当出现连续的数组和大于前面出现的，就重置
        }
        return end;
    }
};
```

```cpp
class Solution //采用分治的思想，同时操作四个数据
{              //使用结构体的方式，使函数可以同时输出多个数据
public:
    struct Status {
        int lSum, rSum, mSum, iSum;
    };

    Status pushUp(Status l, Status r) {
        int iSum = l.iSum + r.iSum;
        int lSum = max(l.lSum, l.iSum + r.lSum);
        int rSum = max(r.rSum, r.iSum + l.rSum);
        int mSum = max(max(l.mSum, r.mSum), l.rSum + r.lSum);
        return (Status) {lSum, rSum, mSum, iSum};
    };

    Status get(vector<int> &a, int l, int r) {
        if (l == r) {
            return (Status) {a[l], a[l], a[l], a[l]};
        }
        int m = (l + r) >> 1;  //右移一个单位，相当于除二取整
        Status lSub = get(a, l, m);
        Status rSub = get(a, m + 1, r); //一直循环操作下去直到l=r
        return pushUp(lSub, rSub);      //l=r后再层层叠加回去
    }

    int maxSubArray(vector<int>& nums) {
        return get(nums, 0, nums.size() - 1).mSum;
    }
};
//复杂度为nlogn
```


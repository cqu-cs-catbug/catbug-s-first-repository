### 快速排序的实现

```cpp
int partition(vector<int>& nums,int l,int r){ //l,r分别表示数组的左右元素索引
    int pivot =nums[r]; //以最右边的元素作为标准
    int i =l-1;
    for(int j=l;j<r;j++){
        if(nums[j]<pivot){
            i+=1;
            swap(nums[i],nums[j]);
        }
    }
    swap(nums[i+1],nums[r]);
    return i+1;
} //快速排序的底层逻辑

void quickSort(vector<int>& nums,int l,int r){
    if(l<r){
        int mid=partition(nums,l,r);
        quickSort(nums,l,mid-1);
        quickSort(nums,mid+1,r);
    } //使用递归，直到只剩一个元素
}
```

**平均情况为 O(nlogn)，最坏情况为 O(n²)，要是想避免最坏情况，需要使用随机数或三数取中**

**三数取中：**

```cpp
int median(vector<int>& nums,int l,int r)//经过交换，nums[mid]是三者的中位数
{
    int mid=l+(r-l)/2;
    if(nums[l]>nums[mid])swap(nums[l],nums[mid]);
    if(nums[mid]>nums[r])swap(nums[mid],nums[r]);
    if(nums[l]>nums[r])swap(nums[r],nums[l]);
    return nums[mid];
}
//在快排的函数中使用 int pivot=int median(nums,l,r) 即可
```

**随机数：**

```cpp
int random(l,r)//取l到r之间的随机数
{
    return l+rand()%(r-i+1);
}
//在快排的原函数加上交换的过程
int index=random(l,r);
swap(nums[index],nums[r]);
int pivot=nums[r];
```

**快速排序的动态演示**

**示例**

我们用数组：

```
[2, 4, 1, 6, 3]
```

- `l=0, r=4`
- 选基准 `pivot = nums[r] = 3`

划分过程：

```
初始: [2, 4, 1, 6, 3]   pivot=3
```

扫描：

- `2 < 3` → 交换到左边 → `[2, 4, 1, 6, 3]`
- `4 > 3` → 不动
- `1 < 3` → 交换到左边 → `[2, 1, 4, 6, 3]`
- `6 > 3` → 不动

最后交换 `pivot` 到正确位置：

```
[2, 1, 3, 6, 4]
          ↑
        pivot
```

现在 `pivot=3` 在正确位置。









